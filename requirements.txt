# --- Phase 1: data layer ---
pandas>=2.0
duckdb>=1.0

# --- Phase 2: RAG + MCP server ---
chromadb>=0.5
mcp>=1.0

# --- Phase 3: LangGraph multi-agent pipeline ---
langgraph>=0.2
langchain>=0.3
langchain-core>=0.3
langchain-google-genai>=2.0
pydantic>=2.0
python-dotenv>=1.0

# Optional: only needed if you set GSR_LLM_PROVIDER=anthropic in .env
# langchain-anthropic>=0.3

# --- Phase 4: FastAPI layer ---
fastapi>=0.115
uvicorn[standard]>=0.30
requests>=2.31   # used by scripts/demo_realtime_signup.py
python-multipart>=0.0.9   # required by FastAPI for file uploads (voice endpoint)

# --- Phase 5: Voice (local, offline -- no API costs) ---
openai-whisper>=20231117   # STT; also requires ffmpeg installed on your system
pyttsx3>=2.90               # TTS; uses Windows SAPI5 / macOS NSSpeechSynthesizer / espeak on Linux

# --- Phase 6: Frontend ---
streamlit>=1.38   # >=1.38 required for the built-in st.audio_input mic widget

# --- Phase 7: caching, concurrency, load testing ---
redis>=5.0   # optional -- session store falls back to in-memory if unreachable
locust>=2.29

# --- Later phases (uncomment as we build them) ---
# langsmith
